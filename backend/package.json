{
  "name": "bookie-backend",
  "version": "1.0.0",
  "description": "Bookie backend API",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --ci --passWithNoTests",
    "lint": "eslint src --ext .js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
